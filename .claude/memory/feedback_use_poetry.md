---
name: Use poetry for dependency management
description: User prefers poetry over venv/pip for Python project setup
type: feedback
---

Use poetry for dependency management and virtual environments, not raw venv + pip.

**Why:** User preference for project tooling.
**How to apply:** Always use `poetry install`, `poetry run`, and `poetry add` for this project and likely future Python projects.
